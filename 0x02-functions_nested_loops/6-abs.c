#include "main.h"
/**
 * _abs - return int absolute value
 * @n: charecter to print abs
 * Return: It return abs n
 */

int _abs(int n)
{
	if (n < 0)
	{
		return (n * (-1));
	}
	else
	{
		return (n);
	}
}
